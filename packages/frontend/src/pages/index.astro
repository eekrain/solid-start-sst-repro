---
import { trpcVanillaClient } from '../utils/trpc';

// const tes = await trpcVanillaClient.user.hello.query({name: "Kontol"})
const tes = await trpcVanillaClient.user.hello.query({name: "Ardian"})
console.log("🚀 ~ file: index.astro:9 ~ tes:", tes)

---

<!DOCTYPE html>
<html lang="en">
	<body>
		<main>
			<h1 class="font-medium text-3xl text-blue-600">🧑‍🚀 Hello, Astronaut!</h1>
    </main> 
	</body>
</html>
